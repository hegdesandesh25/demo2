pipeline {
      agent any
      stages {

            stage('Build') {
                  steps {
                        echo 'Building Sample Maven Project'
                  }
            }
            
      }
}
